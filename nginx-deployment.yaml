apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kubernetes.io/change-cause: "add resources"
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - image: nginx
          name:  web
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              CPU: "200m"
            limits:
              memory: "256Mi"
              CPU: "300m"

